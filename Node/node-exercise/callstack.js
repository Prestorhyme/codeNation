// const x = 1;
// const y = x + 2;

// console.log('sum is ') + plus;

// const z = 20;


const myLocations = ['Manchester', 'London'];

const listLocations = (locartionsArray) => {
    locartionsArray.forEach((location) => {
        console.log(location)
    });
}

listLocations(myLocations)